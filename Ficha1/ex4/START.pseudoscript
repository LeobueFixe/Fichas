entrada("Insira dois números separado por um espaço:", numero)

lista = []
separar numero por " " e guardar em lista[]

se numeros em lista NÃO INT OU FLOAT OU lista[2] == true:
    imprime("Tente novamente.")
senao:
    se lista[0] < lista[1]:
        imprime("O maior número é:{lista[1]}.")
    senao numero1 > numero2:
        imprime("O maior número é:{lista[0]}.")
    senao:
        imprime("Os números são iguais.")