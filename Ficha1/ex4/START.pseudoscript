lista = []
valido = false

enquanto valido = false:
    entrada("Insira o primeiro número:", numero1)
    entrada("Insira o segundo número:", numero2)
    se numero2 NAO INT OU numero2 NAO FLOAT OU numero1 NAO INT OU numero1 NAO FLOAT:
        imprime("Erro, tente novamente.")
        continua()
    valido = true

se lista[0] < lista[1]:
    imprime("O maior número é:{lista[1]}.")
senao numero1 > numero2:
    imprime("O maior número é:{lista[0]}.")
senao:
    imprime("Os números são iguais.")
