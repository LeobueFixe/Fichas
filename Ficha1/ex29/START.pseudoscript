entrada("Insira um número:", numero)

contador = numero

se numero Não INT OU numero < 0:
    imprie("Tente novamente.")
senao:

    enquanto contador < 1:
        total = total + (numero * contador)
        contador--
    
    imprime("O fatorial de {numero} é {total}.")
