entrada("Insira um número:", numero)
total = 0
contador = 1
se numero NÃO INT:
    imprime("Tente novamente.")
senao:
    enquanto numero < total:
        total = contador * (contador + 1) / 2
        contador++
    se numero == total:
        imprime("O número é triangular.")
    senao numero > total:
        imprime("O número não é triangular.")            
